# 截图与录屏 / Screenshots

自述文档与 `documents/` 引用的图片放在这里。

Images referenced by the READMEs and by `documents/`.

| 文件 / File | 内容 / What it shows |
|---|---|
| `console-zh.gif` | 控制台中文界面：总览 → 身份池 → 调度器 → 调试台（解析一条真实链接）→ 资料库 → 下载 → 接口文档 → MCP |
| `console-en.gif` | The same tour of the console, in English |

录制说明：1000×522，2 fps，用 Chrome 录制后由 ffmpeg 裁边、缩放并做调色板优化。
录制内容全部来自本地实例，不含 cookie、msToken、API Key、代理地址或任何可用于冒充身份的凭据。

Recorded at 1000×522, 2 fps: captured in Chrome, then cropped, scaled and
palette-optimised with ffmpeg. Everything shown comes from a local instance and
carries no cookie, msToken, API key, proxy address or any other credential.
